# shift-pilot-resa-web

Pilote de test SHIFT/Paperclip — interface de réservation de transferts inter-îles.
Consomme `shift-pilot-resa-api` (même projet, dépôt séparé).

## Stack
HTML + JS natif, aucune dépendance, aucun build.
